import ThemeProvider from '../ThemeProvider'

export default function ThemeProviderExample() {
  return (
    <div className="p-4">
      <p className="text-foreground">Theme Provider is automatically applied to the app</p>
    </div>
  )
}